<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Property for correlation ID key -->
    <property name="CORRELATION_ID_KEY" value="correlationId"/>
    
    <!-- Console appender with JSON format for CloudWatch -->
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- Base fields -->
            <jsonGeneratorDecorator class="net.logstash.logback.decorate.JsonGeneratorDecorator"/>
            
            <!-- Include standard fields -->
            <includeMdcKeyName>${CORRELATION_ID_KEY}</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <includeMdcKeyName>organisationId</includeMdcKeyName>
            <includeMdcKeyName>venueId</includeMdcKeyName>
            <includeMdcKeyName>requestUri</includeMdcKeyName>
            <includeMdcKeyName>requestMethod</includeMdcKeyName>
            <includeMdcKeyName>responseStatus</includeMdcKeyName>
            <includeMdcKeyName>xrayTraceId</includeMdcKeyName>
            
            <!-- Custom fields -->
            <customFields>{"service":"food-cost-calculator-api"}</customFields>
            
            <!-- Field names mapping -->
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <thread>thread</thread>
                <level>level</level>
                <levelValue>[ignore]</levelValue>
            </fieldNames>
            
            <!-- Timestamp format -->
            <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</timestampPattern>
            <timeZone>UTC</timeZone>
        </encoder>
    </appender>
    
    <!-- Console appender with human-readable format for local development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{${CORRELATION_ID_KEY}:-NO_CORRELATION_ID}] - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Spring profile-specific configuration -->
    <springProfile name="local">
        <!-- Use human-readable logs for local development -->
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.cogschecker.foodcost" level="DEBUG"/>
        <logger name="org.springframework.security" level="DEBUG"/>
    </springProfile>
    
    <springProfile name="dev,staging,prod">
        <!-- Use JSON logs for AWS environments -->
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
        <logger name="com.cogschecker.foodcost" level="INFO"/>
        <logger name="org.springframework.web" level="INFO"/>
        <logger name="io.awspring.cloud" level="INFO"/>
        <logger name="software.amazon.awssdk" level="WARN"/>
        <logger name="com.amazonaws.xray" level="INFO"/>
    </springProfile>
</configuration>
